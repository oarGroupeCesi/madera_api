machine:

  timezone:
    Europe/Paris

  php:
    version: 5.6.14

  services:
    - mysql

dependencies:
  post:
    - php -r "copy('.env.example', '.env');"
    - php artisan key:generate

database:

  override:
    - php artisan migrate --env=testing