machine:

  timezone:
    Europe/Paris

  php:
    version: 5.6.14

  services:
    - mysql

  environment:
    ENVIRONMENT: testing
    DB_URL: 127.0.0.1
    DB_NAME: circle_test
    DB_USER: ubuntu

database:

  override:
    - php artisan migrate --env=$ENVIRONMENT